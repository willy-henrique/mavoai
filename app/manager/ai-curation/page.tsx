import { CurationModule } from "@/components/manager/curation-module"

export const metadata = {
  title: "Curadoria de IA — Mavo",
}

export default function AiCurationPage() {
  return <CurationModule />
}
